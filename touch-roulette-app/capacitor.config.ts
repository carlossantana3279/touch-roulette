import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
	appId: "com.gmail.cssoftware3279.Touch_Roulette",
	appName: "Touch Roulette",
	webDir: "dist",
};

export default config;
